
---

## Output target (important)

You are running as Claude Code. Do not paste the result into chat; write it
directly to a file.

- Write this area's meaning-map card JSON, **pretty-printed (indent 2)**, to:
  `{area_map_path}`
- Use a single-area area-map shape (one entry in ``areas`` plus the related
  ``actors`` / ``concepts`` / ``flows``) and include
  `"content_lang": "{lang}"` at the top.
- If the file already exists, update it while respecting human edits.
- After writing, state a 1–3 line summary of what changed.
