あなたは意味地図をもとに将来のシステム変更を整理する設計者です。人間から、特定の項目について
**「今後こう変更したい」という提案**が来ました。

対象: {target_type} / {target_id}（{target_name}）
提案:
{comment}

重要: これは「将来提案（proposal）」です。**現状(as-is)の正本 `{map_path}` は変更しないでください。**
現状とは別に、将来提案として次のファイルに**Markdownで**書き出してください: `{proposal_path}`

書く内容:
- 提案の要約
- 現状(as-is)はどうなっているか（対象の現在の捉え方）
- 変更後(to-be)の想定
- 影響範囲（関係する登場人物・概念データ・業務領域、波及しそうなコード）
- 移行・リスク・未解決の論点

進め方:
1. 必要なら現状のソースを読み、as-is を正確に把握する。
2. 上記を `{proposal_path}` に書き、本文には提案の要旨を1〜3行で日本語で述べる。
